{
  "name": "linesync-relay",
  "version": "1.0.0",
  "description": "LineSync relay server — routes sync messages between VS Code peers",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
